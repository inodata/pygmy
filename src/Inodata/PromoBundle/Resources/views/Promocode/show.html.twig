{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Promocode</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Confirmation_token</th>
                <td>{{ entity.confirmationtoken }}</td>
            </tr>
            <tr>
                <th>Downloads</th>
                <td>{{ entity.downloads }}</td>
            </tr>
            <tr>
                <th>Is_valid</th>
                <td>{{ entity.isvalid }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('promocode') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('promocode_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('promocode_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
